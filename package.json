{
  "name": "truelensai",
  "private": true,
  "version": "1.0.0",
  "description": "TrueLensAI - backend API + Chrome extension",
  "workspaces": [
    "backend",
    "extension"
  ],
  "scripts": {
    "backend:start": "npm --prefix backend run start",
    "backend:dev": "npm --prefix backend run dev",
    "extension:typecheck": "npm --prefix extension run typecheck",
    "build": "npm --prefix extension run build"
  },
  "devDependencies": {
    "webpack": "^5.104.1",
    "webpack-cli": "^6.0.1"
  }
}
